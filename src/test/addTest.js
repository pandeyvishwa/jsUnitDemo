		function testGetPOICategories_CategoryNameSubString() {
	
		var readVal = add(2, 2);
		// 1. return full result array and loop in the test case to match or assert.
		// 2. size (result.length)
		// 3. id, name, description
		assertEquals("testFunction has func1","4" , add.toString());
		}